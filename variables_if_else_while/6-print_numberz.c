#include <stdio.h>
/**
* main - prints a random number from 0 to 9
* Description: Prints a random number taken from a variable
* Return: 0
 */
int main(void)
{
		int c = 48;

		for (c = 48 ; c <= 57 ; c++)
			putchar(c);
		putchar('\n');
		return (0);
}
